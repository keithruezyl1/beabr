- add notification for registry owner when someone joins an open coordination registry
- when registry is set to private, add a reveal time (which is also when the registry closes)
- when registry is private, use reveal time in the "Reveal Opens" component in the main registry page
- when registry is open, add a closing time (which is when the registry ends)
- when registry is private, use closing time in the "Reveal Opens" field, now replaced by "Registry closes at"
- when a registry closes for either modes: owner cannot add items, participants cannot reserve/prepare/initiate pledges, if registry is private everyone is now revealed like its an open coordination

- when registry ends, when visiting the registry page for the first time after it ends, show the success modal with title and subtitle (set ONE, EVENT-CATEGORY APPROPRIATE TITLE and SUBTITLE per category and use that)
- remove the thank you feature
- move registry to ended 

- add preview for registry participants visible for both registry owner and participant
- test end to end
- caching
- create exportable cards
